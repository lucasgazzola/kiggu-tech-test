export enum UserRole {
  ANALYST = "ANALYST",
  ADMIN = "ADMIN",
}

export enum TermType {
  BRAND = "BRAND",
  DOMAIN = "DOMAIN",
  KEYWORD = "KEYWORD",
}

export enum Severity {
  LOW = "LOW",
  MED = "MED",
  HIGH = "HIGH",
  CRITICAL = "CRITICAL",
}

export enum EventStatus {
  NEW = "NEW",
  REVIEWED = "REVIEWED",
  CLOSED = "CLOSED",
}
